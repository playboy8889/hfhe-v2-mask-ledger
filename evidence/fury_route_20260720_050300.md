# FURY route triage — 2026-07-20

A public technical article describes a conditional attack on the native quadratic PRF used by the recrypt path. The route requires a target-specific public `Rku`/`rk.sec` bundle. The local v2 challenge artifact set contains no such bundle, so this is recorded as a conditional method lead, not a recovered key or plaintext.

The source anchor is the public `pvac_hfhe_cpp` commit `e2835df34ebbc510d3f8c93062dc5bf5bc34d2dc`. No secret values are copied here.
